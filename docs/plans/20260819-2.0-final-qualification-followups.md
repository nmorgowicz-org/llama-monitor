# Local LLM Foundry 2.0 — Final qualification follow-ups

**Status:** Focused post-merge checklist; the rebrand, bounded Calibration v1, Calibration UX, and native Windows core qualification are complete and archived.  
**Scope:** Only release and residual platform evidence required after PR #314.  
**Authority:** The archived execution records under `docs/archive/rebrand/`, `docs/archive/calibration/`, and `docs/archive/windows/` are the historical evidence. This document is the active list of remaining work.

## Current gate

PR #314 is mergeable: required Rust, JavaScript, UI E2E, CodeQL, Linux/macOS release-smoke, and Windows-target/native release checks are green. No code change is implied by this plan unless a probe finds a real regression.

## Work remaining

### 1. Release-please and artifact cutover (post-merge)

- Confirm release-please opens the expected `2.0.0` major release PR from the `feat!:` title.
- Verify canonical `local-llm-foundry` artifacts and retained legacy aliases are both published during 2.x.
- Verify checksums, archive contents, executable names, icon/manifest registration, and updater selection for canonical and legacy assets.
- Exercise one clean install/upgrade and one legacy-user auto-upgrade using disposable roots; retain redacted receipts.
- Record the 2.1 filename-cutover decision separately; do not remove 2.x aliases early.

### 2. Windows residual receipts

Run on the native Windows machine using repository-relative commands and disposable roots:

- Interrupted migration journal recovery, including restart and rollback after interruption.
- Sensor-bridge healthy, missing, and recovery states.
- Updater replacement, checksum verification, archive extraction, restart, and rollback.
- Remote-agent install, update, uninstall, and mixed-version task cleanup.
- Full ZIP/package/icon manifest inspection (not only a temporary archive).
- Screenshot parity acceptance for shared core/config surfaces at the agreed viewport/theme matrix. Rapid-MLX executable/runtime captures remain Apple-Silicon-only; mocked UI coverage remains cross-platform.

Each receipt must include commit, OS/build identity, exact command, redacted output, produced-file manifest, and SHA-256 where applicable. A failed probe remains open; it must not be marked skipped.

### 3. Final security and release-contract sign-off

- Re-run the security checklist against the merged tree: auth, constant-time token checks, path validation, SQLite backup APIs, no secret logging, and bounded expensive operations.
- Run release preflight and link its output to the 2.0.0 artifact receipt.
- Confirm canonical URLs, migration docs, updater compatibility notes, and legacy alias policy agree.

## Explicitly deferred

Broad MoE optimization, overnight/custom calibration searches, and broader Rapid-MLX calibration are not 2.0 blockers. They require a separately scoped plan and new evidence rather than reopening the completed Calibration v1 work.

## Closure criteria

Close this plan when the release-please/artifact probes and all selected Windows residual receipts pass, the final security/release-contract review is recorded, and the canonical 2.0.0 artifact set is reproducible. Until then, keep this file in `docs/plans/` and update the checklist with dates and receipt links.
