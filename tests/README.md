# Test fixtures

`fixtures/rapid_mlx/` contains bounded, static endpoint examples used by the
Rapid-MLX telemetry contract tests. They intentionally cover idle/generating status,
unknown fields, exact GiB conversion, recognized vision cache data, and the text-only
cache fallback. Tests must not require a live Rapid-MLX installation.
